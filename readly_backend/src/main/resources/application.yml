spring:
  datasource:
    url: jdbc:mysql://mysql:3306/readly
    username: ehddls
    password: ehddls
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        format_sql: true
        # use_sql_comments: true
  data:
    redis:
      host: redis
      port: 6379
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
